<h1> <%= @user.username %>'s user page </h1>

<h2> <%= @user.username %>'s favorite exercises </h2>

<% @favorites.each do |fav| %>
  <li><%= link_to "#{fav.name}", exercise_path(fav) %></li>
<% end %>